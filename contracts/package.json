{
  "name": "veragig-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:celo": "hardhat run scripts/deploy.ts --network celo",
    "deploy:escrow:celo": "hardhat run scripts/deploy-escrow.ts --network celo",
    "deploy:escrow:local": "hardhat run scripts/deploy-escrow.ts --network localhost",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "verify:celo": "hardhat run scripts/verify-deploy.ts --network celo",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
